EzMap is a simple GUI application to convert Heightmaps into .MAP files primarily for Return To Castle Wolfenstein mapping. 

Requires PyQt6 & Pillow
(Install:  pip install PyQt6 Pillow)

run tool.py or runtool.bat > import your created heightmap image > click Generate .MAP > once generated click Save Terrain. 

todo:

write a propper readme